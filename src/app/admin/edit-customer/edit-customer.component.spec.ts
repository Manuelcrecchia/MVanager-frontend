import { EditCustomerComponent } from './edit-customer.component';

describe('EditCustomerComponent', () => {
  it('should be exported', () => {
    expect(EditCustomerComponent).toBeTruthy();
  });
});

import { UserSettingsComponent } from './user-settings.component';

describe('UserSettingsComponent', () => {
  it('should be exported', () => {
    expect(UserSettingsComponent).toBeTruthy();
  });
});

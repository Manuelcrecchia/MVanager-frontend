import { LeaveSettingsComponent } from './leave-settings.component';

describe('LeaveSettingsComponent', () => {
  it('should be exported', () => {
    expect(LeaveSettingsComponent).toBeTruthy();
  });
});

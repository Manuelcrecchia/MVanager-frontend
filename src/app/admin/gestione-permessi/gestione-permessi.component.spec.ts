import { GestionePermessiComponent } from './gestione-permessi.component';

describe('GestionePermessiComponent', () => {
  it('should be exported', () => {
    expect(GestionePermessiComponent).toBeTruthy();
  });
});

import { GestioneAssenzeComponent } from './gestione-assenze.component';

describe('GestioneAssenzeComponent', () => {
  it('should be exported', () => {
    expect(GestioneAssenzeComponent).toBeTruthy();
  });
});

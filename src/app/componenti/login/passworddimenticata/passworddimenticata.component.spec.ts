import { PassworddimenticataComponent } from './passworddimenticata.component';

describe('PassworddimenticataComponent', () => {
  it('should be exported', () => {
    expect(PassworddimenticataComponent).toBeTruthy();
  });
});

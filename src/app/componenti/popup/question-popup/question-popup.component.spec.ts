import { QuestionPopupComponent } from './question-popup.component';

describe('QuestionPopupComponent', () => {
  it('should be exported', () => {
    expect(QuestionPopupComponent).toBeTruthy();
  });
});

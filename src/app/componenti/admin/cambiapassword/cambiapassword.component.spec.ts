import { CambiapasswordComponent } from './cambiapassword.component';

describe('CambiapasswordComponent', () => {
  it('should be exported', () => {
    expect(CambiapasswordComponent).toBeTruthy();
  });
});

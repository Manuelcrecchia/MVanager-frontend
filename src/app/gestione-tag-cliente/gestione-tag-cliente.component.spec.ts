import { GestioneTagClienteComponent } from './gestione-tag-cliente.component';

describe('GestioneTagClienteComponent', () => {
  it('should be exported', () => {
    expect(GestioneTagClienteComponent).toBeTruthy();
  });
});

import { InternalDocumentsService } from './internal-documents.service';

describe('InternalDocumentsService', () => {
  it('should be exported', () => {
    expect(InternalDocumentsService).toBeTruthy();
  });
});

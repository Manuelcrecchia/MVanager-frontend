import { AppointmentModelService } from './appointment-model.service';

describe('AppointmentModelService', () => {
  it('should be exported', () => {
    expect(AppointmentModelService).toBeTruthy();
  });
});
